import 'package:flutter/material.dart';

class WeatherNoConnectvity extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('No Connectvity'),
      ),
    );
  }
}